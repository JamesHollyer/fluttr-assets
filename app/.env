# Supabase project used for accounts and sync. The anon key is public by design;
# row-level security on the server protects each user's data. Leave empty to
# build without sync. Values are read at bundle time: restart Metro after editing.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
